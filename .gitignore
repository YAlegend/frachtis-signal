# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Local config & secrets
.env

# Runtime artifacts
*.db
*.db-journal

# Local-only application docs + real-portfolio ground truth (kept on disk, never committed).
docs/AI_Agent_Builder_Brief.md
docs/Signal_Application_Brief.md
data/backtest/ground_truth.portfolio.json
data/backtest/ground_truth.portfolio.filled.json

# Keep out/ sample digests in the repo as examples; ignore everything else generated.
